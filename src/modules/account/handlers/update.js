'use strict';
/**
 * update
 *
 * patch method called on /account/{accountId}
 */
var Boom = require('boom');

var models = require('../models');


module.exports = function(request, reply) {
    console.log('account.handlers.update');

};
